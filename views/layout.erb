<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta property="og:site_name" content="Library" />
    <link rel="shortcut icon" href="/favicon.ico">

    <%= meta_property "og:title" %>
    <%= meta_name :author %>
    <%= meta_name :description %>
    <%= meta_property "og:description" %>
    <%= meta_property "og:type" %>
    <%= meta_property "og:image" %>
    <%= meta_name "twitter:card" %>
    <%= meta_property "twitter:image" %>
    <%= meta_property "twitter:title" %>

    <title><%=h [@title, 'LIBRARY'].compact.join ' | ' %></title>
    <link rel="stylesheet" href="/style.css">
    <style>
     .cover {
         box-sizing: border-box;
         border-radius: 0px 0.3em 0.3em 0px;
         border-left: 1px solid #ccc;
         box-shadow: 0.1em 0.1em #ddd,
         0.1em 0.2em #cdcdcd,
         0.1em 0.3em #ddd,
         0.1em 0.4em #cdcdcd,
         0.1em 0.5em #ddd,
         0.1em 0.6em #cdcdcd,
         0.2em 0.7em #ccc,
         0em 0.7em #ccc;
	   }
    </style>
  </head>
  <body>

    <section class="hero">
      <div class="hero-head">
        <%= erb :'partials/user_nav' if loggedin? %>
      </div>

      <div class="hero-body has-text-centered">
        <%= erb :'partials/header', locals: { user: user }  %>
      </div>
    </section>


    <section class="section">
      <div class="container is-max-widescreen">
        <%= yield %>
      </div>
    </section>

    <footer class="footer">
      <div class="content has-text-centered">
        <a href="https://github.com/emad-elsaid/library" rel="noopener" target="_blank">Source code on Github</a> •
        <a href="/privacy">Privacy</a>
      </div>
    </footer>

  </body>
</html>
